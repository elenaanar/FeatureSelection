#include "Validator.h"

// Function to validate the classifier
double Validator::validate(vector<int> featureSubset, string dataLoc) {
    //TODO: Implement the validation function
    // Create a classifier object with the feature subset

    // Train the classifier with the data

    // Evaluate the classifier with the feature subset

    // Return the accuracy of the classifier
    return 0.0;
}

int Validator::evaluate(int id, Classifier& classifier) {
    //TODO: Implement the evaluation function
    // Test the classifier with the given id

    // 1 if the classifier is correct, 0 if the classifier is incorrect
    return 0;
}